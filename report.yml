---
- name: "Ansible Migration Factory - Report"
  hosts: "all"
  gather_facts: False
  tasks:
    - name: "Generate report"
      block:
        - name: "Ensure report directory exists"
          file:
            state: "directory"
            path: "/var/lib/awx/reports"
            mode: "0755"
            owner: "awx"
            group: "awx"

        - name: "Create report"
          template:
            src: "simple_report.csv.j2"
            dest: "/var/lib/awx/reports/simple_report.csv"
            owner: "awx"
            group: "awx"
            mode: "0644"
      delegate_to: "localhost"
      run_once: True
